/**
 * **A function that sets any string into the page title.**
 * @param page_title {string} The page title as a string.
 */
const setPageTitle = (page_title: string) => {
    document.title = page_title
}

export default setPageTitle